"use strict";

let incr = 10,
	decr = 10;

// incr++;
// decr--;

// console.log(`incr = ${incr++}, decr = ${decr--}`)

// ++incr;
// --decr;

console.log(`incr = ${++incr}, decr = ${--decr}`)

const isChecked = false,
	isClose = false;

console.log(isChecked || isClose);
